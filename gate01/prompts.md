# prompts.md — AI への指示ログ

使用ツール：Claude（Chrome の Claude サイドパネル、エージェントモード）
AI には配布資料 PDF（Gate1_配布資料_粒度100.pdf）を Google ドライブから直接読み込ませ、要件定義書を丸ごと渡しました。

---

## Prompt 1

> このサイトがVEXUMっていう長期インターン先の昇給条件的な奴でこれを一気に終わらせるために手伝ってほしいんだけどできそう？

AI の応答（要約）：NEXUS の画面を読み取り、Gate1 の課題内容と「AI に渡して実装→自分で検証」の流れを確認。検証とセルフチェックは自分でやるべき部分だと提案され、要件定義書と GitHub の準備を求められた。

## Prompt 2

> 開いた

（配布資料 PDF のタブを開いた状態で送信）

AI の応答（要約）：
1. PDF を読み、`docs/spec_breakdown.md` に実装単位（遷移表・付随処理・R-01〜06・V-01〜07・メッセージ・N-01〜03）へ分解。メッセージ表の R-01/R-02 の対応と「200文字」の空白は原本 PDF の画像で確認。
2. Flask + SQLite で実装（`app.py` / `init_db.py` / `templates/`）。
3. セルフチェック13項目を pytest で自動化（`tests/test_selfcheck.py`、17件すべて合格）。
4. Playwright でモック画像5枚を撮影。
5. README の雛形と本ファイルを作成。

---

## Prompt 3〜5（リポジトリ準備）

> githubのリポジトリ新しくこれ用を作ってくれる？
> gate1ってしなくていい気がするんだけどここのリポジトリに今後全部のゲート入れて良くない？整理は大事だと思うけど

AI の応答（要約）：GitHub に Public リポジトリ `nexus55` を作成し、1ゲート＝1フォルダ（gate01/）の構成にした。Gate2 以降はゲートごとに PR で提出する方針にした。

## Prompt 6（セルフチェック・振り返り）

> これさ、正直99％AIでやってそれのやったこととかをメモリで蓄積するだけでいい気がするから正味俺がチェックとかする必要ない気がするんだけど
> おけ今後のgateもこれまでAIでやるからこれどうなのって聞かれたときに対応できるように俺が把握しておかないといけない内容とかをまとめたりこれは俺が見たほうがいい的なのを最初に話すようにしておくか

AI の応答（要約）：セルフチェックは自動テストの結果をもとに記入し、確認方法も明記した。振り返り 6-1・6-2 は AI が下書きし、自分で確認して修正。6-3 は自分で記入。

<!-- 以降、修正依頼などで AI に投げた指示を追記していく -->
